<sql>

	<statement>
    CREATE TABLE IF NOT EXISTS QUESTION (
		ID_QUESTION INTEGER PRIMARY KEY,
        DS_QUESTION VARCHAR(100),
		OPT1 VARCHAR(4),
		OPT2 VARCHAR(4),
		OPT3 VARCHAR(4),
		OPT4 VARCHAR(4),
		SCORE INTEGER,
		OPT_CORRECT_ANSWER INTEGER
	);
    </statement>
    
    
	<statement>
    CREATE TABLE IF NOT EXISTS QUESTION_ANSWER (
		ID_QUESTION_ANSWER INTEGER PRIMARY KEY,
        ID_QUESTION INTEGER PRIMARY KEY,
        DS_ANSWER VARCHAR(100),
		CK_CORRECT VARCHAR(1)
	);
    </statement>
    
</sql>